
/*#ifndef UTILS_H_
#define UTILS_H_

#include <stdlib.h>
#include <stdio.h>
#include <errno.h>

#include "linked_list.h"

typedef struct {
    ssize_t w_len;
    List* list;
}word_list;

typedef struct {
    ssize_t count;
    char* string;
} Word;

#endif  // UTILS_H_*/